// import axios from 'axios';

// import { serverUrl } from '../clientConfig.js';

export default {
    initData: function(context, payload) {
        console.log(`getting operation data for ${payload.role} ${payload.CUS_NO}`);
        return 1;
    }
};
